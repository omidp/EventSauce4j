spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/testdb
    username: test
    password: test
  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

eventsauce4j:
  outbox:
    lockName: payment_outbox_lock
    delayInterval: 10 #seconds
    enabled: true
  rabbitmq:
    host: 127.0.0.1