spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/testdb
    username: test
    password: test
  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8081



eventsauce4j:
  outbox:
    lockName: user_outbox_lock
    delayInterval: 10 #seconds
    enabled: false
  rabbitmq:
    host: 127.0.0.1